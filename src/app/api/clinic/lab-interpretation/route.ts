import { NextRequest, NextResponse } from "next/server";
import { withProductAuth } from "@/lib/nx/product-auth";
export const runtime="nodejs";export const dynamic="force-dynamic";
const RANGES: Record<string,{low:number;high:number;unit:string;meaning:string;advice:string}>={"Hemoglobin":{low:12,high:17,unit:"g/dL",meaning:"Low Hb indicates anemia — very common in India (NFHS-5: 57% women)",advice:"Iron supplements + iron-rich diet (green leafy veg, jaggery)"},"Fasting Blood Sugar":{low:70,high:100,unit:"mg/dL",meaning:">126 = diabetes (ICMR-INDIAB criteria)",advice:">126: consult doctor for diabetes management"},"HbA1c":{low:4,high:5.7,unit:"%",meaning:"5.7-6.4 = prediabetes, ≥6.5 = diabetes (ICMR target <7%)",advice:">6.5: diabetes treatment needed"},"TSH":{low:0.4,high:4.0,unit:"mIU/L",meaning:">4.0 = hypothyroidism, <0.4 = hyperthyroidism",advice:">4.0: start Levothyroxine, recheck TSH in 6 weeks"},"Total Cholesterol":{low:120,high:200,unit:"mg/dL",meaning:">200 = dyslipidemia",advice:">200: statin therapy + diet modification"},"Creatinine":{low:0.6,high:1.2,unit:"mg/dL",meaning:">1.2 = possible kidney impairment",advice:">1.5: nephrology referral"}};
async function POST_impl(req:NextRequest){try{const b=await req.json().catch(()=>({}));const test=(b?.test||"").trim();const value=Number(b?.value);if(!test||isNaN(value))return NextResponse.json({error:"missing"},{status:400});const range=RANGES[test];if(!range)return NextResponse.json({interpretation:null,message:"No reference range available for this test"});const status=value<range.low?"LOW":value>range.high?"HIGH":"NORMAL";return NextResponse.json({test,value,status,unit:range.unit,range:`${range.low}-${range.high}`,meaning:range.meaning,advice:range.advice,indianContext:true,source:"ICMR + NFHS-5 Indian reference ranges"});}catch(e){return NextResponse.json({error:"failed"},{status:500});}}

export const POST = withProductAuth("clinic.lab-interpretation.POST", POST_impl);
