import { NextRequest, NextResponse } from "next/server";
export const runtime="nodejs";export const dynamic="force-dynamic";
const AUTO_ANSWERS: Record<string,string>={"side effects":"Common side effects include nausea, dizziness, or drowsiness. If severe, stop medication and consult your doctor immediately.","dosage":"Take the medicine as prescribed by your doctor. Do not skip or double doses. If you miss a dose, take it as soon as you remember unless it's close to the next dose.","diet":"Maintain a healthy diet with plenty of fruits, vegetables, and water. Avoid spicy or oily food if you have gastric issues. Low salt diet if hypertensive.","timing":"Take morning medicines on an empty stomach (30 min before food) unless your doctor advised otherwise.","duration":"Complete the full course of antibiotics even if you feel better. For other medicines, follow the duration prescribed."};
export async function POST(req:NextRequest){try{const b=await req.json().catch(()=>({}));const q=(b?.question||"").toLowerCase();if(!q)return NextResponse.json({error:"no_question"},{status:400});for(const[key,answer]of Object.entries(AUTO_ANSWERS)){if(q.includes(key)){return NextResponse.json({answer,source:"AI auto-answer",escalated:false});}}return NextResponse.json({answer:"I'm not sure about that. I'll forward your question to the doctor who will respond shortly.",source:"escalated to doctor",escalated:true});}catch(e){return NextResponse.json({error:"failed"},{status:500});}}
