import { NextRequest, NextResponse } from "next/server";
export const runtime = "nodejs"; export const dynamic = "force-dynamic";
const PLANS: Record<string,{disease:string;checkups:{test:string;frequency:string;guideline:string}[];reminders:string[]}>={"Diabetes Type 2":{disease:"Diabetes Type 2",checkups:[{test:"HbA1c",frequency:"Every 3 months",guideline:"ICMR target <7%"},{test:"Foot exam",frequency:"Annually",guideline:"ICMR diabetic foot protocol"},{test:"Eye exam (fundus)",frequency:"Annually",guideline:"ICMR retinopathy screening"},{test:"Urine microalbumin",frequency:"Annually",guideline:"ICMR nephropathy screening"}],reminders:["Take Metformin with meals","Check blood sugar before meals","Annual flu vaccine"]},"Hypertension":{disease:"Hypertension",checkups:[{test:"Blood pressure",frequency:"Weekly",guideline:"ICMR target <140/90"},{test:"ECG",frequency:"Annually",guideline:"ICMR cardiac screening"},{test:"Serum creatinine",frequency:"6 months",guideline:"ICMR renal monitoring"}],reminders:["Take BP medication morning","Low salt diet <5g/day","Exercise 30 min daily"]}};
export async function GET(req:NextRequest){try{const{searchParams}=new URL(req.url);const dx=searchParams.get("diagnosis")||"";if(dx){const key=Object.keys(PLANS).find(k=>k.toLowerCase().includes(dx.toLowerCase())||dx.toLowerCase().includes(k.toLowerCase()));if(key)return NextResponse.json({plan:PLANS[key]});return NextResponse.json({plan:null});}return NextResponse.json({plans:Object.keys(PLANS)});}catch(e){return NextResponse.json({error:"failed"},{status:500});}}
