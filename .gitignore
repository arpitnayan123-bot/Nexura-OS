


















!.env.example
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions
# Database
# Dev screenshots
# Git bundles (workspace lock files — never commit)
# Original upload files
# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.
# Skills (internal)
# Skills directory
# Tool results
# Vercel deployment config is committed (vercel.json)
# Video production intermediates
# but .vercel cache is not
# debug
# dependencies
# env files (can opt-in for committing if needed)
# misc
# next.js
# production
# runtime artifacts (stateless/hardening pass)
# testing
# typescript
# vercel
*.bundle
*.dump
*.log
*.pem
*.tsbuildinfo
.DS_Store
.autocommit-last
.claude
.env*
.nx-watchdog.lock
.pnp.*
.pnpm-debug.log*
.vercel
.vercel/
.yarn/*
.z-ai-config
/.next/
/.pnp
/build
/coverage
/out/
/skills/
/skills/
backups/
db/*.db
db/*.db-journal
db/backups/
dev-screens/
dev.log
dev.out.log
download/
local-*
logs/
next-env.d.ts
node_modules
npm-debug.log*
prompt
repo.bundle
scripts/audit/
server.log
test
tool-results/
upload/
video-clips-v2/
video-narration-v2/
video-narration/
video-recordings/
video-screenshots/
yarn-debug.log*
yarn-error.log*

# redis runtime snapshot (server cwd writes it at repo root)
dump.rdb
.guardian-seed-cooldown
prisma/schema.deploy.prisma
